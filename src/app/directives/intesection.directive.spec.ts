import { IntesectionDirective } from './intesection.directive';

describe('IntesectionDirective', () => {
  it('should create an instance', () => {
    const directive = new IntesectionDirective();
    expect(directive).toBeTruthy();
  });
});
